# Experiment 085 verdict

**Reject the changed Expedia query contract and the pooled fitting ceiling.** All 12/12 independent one-city
Luna contexts were protocol-compliant: each issued exactly the three pre-registered heading-anchored searches,
with no reads, retries, fallback, arithmetic, FX conversion, aggregation, or cross-city evidence. The panel
produced zero strict rows. Exact class pages often exposed only a bare `$` (no named currency), while generic,
district, wrong-city, and class-ambiguous results were correctly rejected.

The deterministic pooled audit remains at 89 strict rows across 41 cities, with 20 matched 2-star/3-star cities
and 26 matched 3-star/4-star cities. Both frozen 30-city relationship gates fail; no coefficient, class split,
or product mapping is authorized. The Expedia search/index boundary is not a viable route for closing the
accommodation model-fitting gate under the current strict estimand. Retain the twelve negative source/access
records as reusable evidence and pivot to a materially different anchor or an explicitly amended, independently
validated estimand.
