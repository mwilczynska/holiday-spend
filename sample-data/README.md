Local Wise CSV fixtures can live here during manual import testing.

The app runtime database stays in `data/`. This folder exists to avoid colliding
with `data/travel.db` after the app is flattened to the repo root.
